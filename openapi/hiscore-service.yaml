# Generated by scripts/split_openapi.py — do not edit.
# Source: openapi/combined.yaml
openapi: 3.1.0
info:
  title: RS3 Market — hiscore-service
  version: 2.0.0
  description: 'Endpoints served by **hiscore-service**, extracted from the combined specification.


    This copy exists for debugging one service in isolation. Generate clients from `openapi/combined.yaml`,
    which describes the whole gateway surface.


    Generated by `scripts/split_openapi.py` — do not edit by hand.'
servers:
- url: /
  description: hiscore-service
security: []
paths:
  /health:
    get:
      tags:
      - health
      operationId: getHealth
      summary: Gateway liveness
      responses:
        '200':
          description: Gateway is up.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthStatus'
  /hiscore/{name}:
    get:
      tags:
      - hiscore
      operationId: getPlayerHiscore
      summary: Skill levels for a player
      description: 'Served from cache when fresh. If the upstream is unreachable but

        we hold a stale copy, the stale copy is returned — check

        `fetched_at`.

        '
      parameters:
      - name: name
        in: path
        required: true
        description: RS3 display name.
        schema:
          type: string
      - $ref: '#/components/parameters/HiscoreMode'
      responses:
        '200':
          description: The player's skills.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Player'
        '404':
          description: Player not found, or the hiscores are unreachable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
tags:
- name: health
  description: Liveness and upstream status.
- name: hiscore
  description: RS3 hiscores lookup.
components:
  parameters:
    HiscoreMode:
      name: mode
      in: query
      description: Hiscore table to read.
      schema:
        $ref: '#/components/schemas/HiscoreMode'
  schemas:
    Error:
      type: object
      required:
      - error
      properties:
        error:
          type: string
          description: Human-readable failure reason.
    HealthStatus:
      type: object
      required:
      - status
      properties:
        status:
          type: string
          example: ok
        service:
          type: string
    HiscoreMode:
      type: string
      enum:
      - normal
      - ironman
      - hardcore
      default: normal
    Player:
      type: object
      required:
      - id
      - name
      - mode
      - fetched_at
      - skills
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        mode:
          $ref: '#/components/schemas/HiscoreMode'
        fetched_at:
          type: string
          format: date-time
          description: 'When the hiscores were last read. May be old if the upstream

            was unreachable and a cached copy was served.

            '
        skills:
          type: array
          items:
            $ref: '#/components/schemas/PlayerSkill'
    PlayerSkill:
      type: object
      required:
      - id
      - player_id
      - skill
      - level
      - xp
      - rank
      properties:
        id:
          type: integer
          format: int64
        player_id:
          type: integer
          format: int64
        skill:
          type: string
          example: Crafting
        level:
          type: integer
        xp:
          type: integer
          format: int64
        rank:
          type: integer
          format: int64
