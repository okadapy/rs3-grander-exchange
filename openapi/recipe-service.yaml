# Generated by scripts/split_openapi.py — do not edit.
# Source: openapi/combined.yaml
openapi: 3.1.0
info:
  title: RS3 Market — recipe-service
  version: 2.0.0
  description: 'Endpoints served by **recipe-service**, extracted from the combined specification.


    This copy exists for debugging one service in isolation. Generate clients from `openapi/combined.yaml`,
    which describes the whole gateway surface.


    Generated by `scripts/split_openapi.py` — do not edit by hand.'
servers:
- url: /
  description: recipe-service
security: []
paths:
  /health:
    get:
      tags:
      - health
      operationId: getHealth
      summary: Gateway liveness
      responses:
        '200':
          description: Gateway is up.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthStatus'
  /recipes:
    get:
      tags:
      - recipes
      operationId: listRecipes
      summary: List recipes
      description: 'Returns full recipes including their inputs. Unpaginated — use

        `/search/recipes` for anything user-facing.

        '
      parameters:
      - name: skill
        in: query
        description: Exact skill name, e.g. `Crafting`.
        schema:
          type: string
      - name: level
        in: query
        description: Upper bound on the recipe's level requirement.
        schema:
          type: integer
          minimum: 1
          maximum: 120
      responses:
        '200':
          description: Matching recipes.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecipeListResponse'
        '500':
          $ref: '#/components/responses/InternalError'
  /recipes/ids:
    get:
      tags:
      - recipes
      operationId: listRecipeOutputItemIds
      summary: Output item IDs for matching recipes
      description: 'Distinct output item IDs, filtered by skill and level range.

        Recipes whose output item ID is still unresolved are excluded,

        so every ID returned here can be priced.

        '
      parameters:
      - name: skill
        in: query
        schema:
          type: string
      - name: min_level
        in: query
        description: Inclusive lower bound on the level requirement.
        schema:
          type: integer
          minimum: 1
          maximum: 120
      - name: level
        in: query
        description: Inclusive upper bound on the level requirement.
        schema:
          type: integer
          minimum: 1
          maximum: 120
      responses:
        '200':
          description: Matching output item IDs.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecipeIdsResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/InternalError'
  /recipes/{itemID}:
    get:
      tags:
      - recipes
      operationId: getRecipeTree
      summary: Recipe tree for an item
      description: 'Resolves every intermediate recipe needed to produce the item.

        Identical to `/recipes/{itemID}/tree`.

        '
      parameters:
      - $ref: '#/components/parameters/ItemIdPath'
      responses:
        '200':
          description: The recipe tree.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecipeTree'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /recipes/{itemID}/tree:
    get:
      tags:
      - recipes
      operationId: getRecipeTreeExplicit
      summary: Recipe tree for an item
      parameters:
      - $ref: '#/components/parameters/ItemIdPath'
      responses:
        '200':
          description: The recipe tree.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecipeTree'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /search/recipes:
    get:
      tags:
      - recipes
      operationId: searchRecipes
      summary: Search recipes by name
      description: 'Paginated recipe summaries. Inputs are **not** included — fetch

        `/recipes/{itemID}` for the full tree.

        '
      parameters:
      - name: q
        in: query
        description: Case-insensitive substring of the recipe or output name.
        schema:
          type: string
      - name: skill
        in: query
        schema:
          type: string
      - name: min_level
        in: query
        schema:
          type: integer
          minimum: 1
          maximum: 120
      - name: level
        in: query
        schema:
          type: integer
          minimum: 1
          maximum: 120
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Offset'
      responses:
        '200':
          description: Matching recipes.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecipeSearchResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/InternalError'
  /search/items:
    get:
      tags:
      - items
      operationId: searchItems
      summary: Search items by name
      description: 'Searches every item that appears as a recipe output or a recipe

        input. `sources` says which side each item was found on.

        '
      parameters:
      - name: q
        in: query
        schema:
          type: string
      - name: source
        in: query
        schema:
          $ref: '#/components/schemas/ItemSource'
      - $ref: '#/components/parameters/Limit'
      - $ref: '#/components/parameters/Offset'
      responses:
        '200':
          description: Matching items.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ItemSearchResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/InternalError'
  /items/limits:
    get:
      tags:
      - items
      operationId: getBuyLimits
      summary: Grand Exchange 4-hour buy limits
      description: 'Items with no known limit are **omitted** from the map rather

        than returned as zero, so "unknown" is distinguishable from

        "limited to nothing".

        '
      parameters:
      - $ref: '#/components/parameters/ItemIdsQuery'
      responses:
        '200':
          description: Known buy limits, keyed by item ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BuyLimitsResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '500':
          $ref: '#/components/responses/InternalError'
tags:
- name: health
  description: Liveness and upstream status.
- name: items
  description: Item lookup and Grand Exchange buy limits.
- name: recipes
  description: Crafting recipes scraped from the RuneScape Wiki.
components:
  parameters:
    ItemIdPath:
      name: itemID
      in: path
      required: true
      description: Grand Exchange item ID.
      schema:
        type: integer
        format: int64
        minimum: 1
      example: 1513
    ItemIdsQuery:
      name: ids
      in: query
      required: true
      description: Comma-separated Grand Exchange item IDs (max 500).
      schema:
        type: string
      example: 1513,1515,2363
    Limit:
      name: limit
      in: query
      description: Page size. Capped at 500.
      schema:
        type: integer
        minimum: 1
        maximum: 500
        default: 50
    Offset:
      name: offset
      in: query
      description: Rows to skip.
      schema:
        type: integer
        minimum: 0
        default: 0
  responses:
    BadRequest:
      description: The request was malformed.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    InternalError:
      description: Unexpected server error.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    NotFound:
      description: No such resource.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
  schemas:
    AphSource:
      type: string
      description: 'Where a recipe''s actions-per-hour came from. `default` means it

        is a house assumption, not a measured rate — the wiki rarely

        publishes one. Treat `default` figures as good for ranking

        recipes against each other, not as literal throughput.

        '
      enum:
      - wiki
      - default
      - override
    BuyLimitsResponse:
      type: object
      required:
      - count
      - limits
      properties:
        count:
          type: integer
        limits:
          type: object
          description: 'Item ID (as a string key) to its 4-hour buy limit. Items

            with no known limit are absent.

            '
          additionalProperties:
            type: integer
          examples:
          - '1513': 25000
            '2363': 10000
    Error:
      type: object
      required:
      - error
      properties:
        error:
          type: string
          description: Human-readable failure reason.
    HealthStatus:
      type: object
      required:
      - status
      properties:
        status:
          type: string
          example: ok
        service:
          type: string
    ItemSearchResponse:
      type: object
      required:
      - query
      - source
      - count
      - total
      - limit
      - offset
      - items
      properties:
        query:
          type: string
        source:
          $ref: '#/components/schemas/ItemSource'
        count:
          type: integer
        total:
          type: integer
          format: int64
        limit:
          type: integer
        offset:
          type: integer
        items:
          type: array
          items:
            $ref: '#/components/schemas/ItemSummary'
    ItemSource:
      type: string
      description: Which side of a recipe an item appears on.
      enum:
      - all
      - output
      - input
      default: all
    ItemSummary:
      type: object
      required:
      - item_id
      - name
      - sources
      properties:
        item_id:
          type: integer
          format: int64
        name:
          type: string
        sources:
          type: array
          description: Which sides of a recipe this item was found on.
          items:
            type: string
            enum:
            - input
            - output
    Recipe:
      type: object
      required:
      - id
      - name
      - output_item_id
      - output_item_name
      - output_qty
      - skill
      - level_req
      - xp_per_action
      - actions_per_hour
      - aph_source
      - ticks
      - members
      - source
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        output_item_id:
          type: integer
          format: int64
        output_item_name:
          type: string
        output_qty:
          type: integer
          description: Units produced per craft.
        skill:
          type: string
        level_req:
          type: integer
          minimum: 1
          maximum: 120
        xp_per_action:
          type: number
          format: double
        actions_per_hour:
          type: integer
        aph_source:
          $ref: '#/components/schemas/AphSource'
        ticks:
          type: integer
          description: Per-action tick cost from the wiki infobox. Zero when the wiki published none,
            or published `varies`.
        facility:
          type: string
          description: Where the recipe is made, such as `Furnace` or `Anvil` — what separates smelting
            from forging within Smithing.
        members:
          type: boolean
        source:
          type: string
          example: runescape.wiki
        inputs:
          type: array
          description: Omitted in summary listings such as `/search/recipes`.
          items:
            $ref: '#/components/schemas/RecipeInput'
    RecipeIdsResponse:
      type: object
      required:
      - skill
      - min_level
      - max_level
      - count
      - item_ids
      properties:
        skill:
          type: string
        min_level:
          type: integer
        max_level:
          type: integer
        count:
          type: integer
        item_ids:
          type: array
          items:
            type: integer
            format: int64
    RecipeInput:
      type: object
      required:
      - id
      - recipe_id
      - item_id
      - item_name
      - quantity
      - is_intermediate
      properties:
        id:
          type: integer
          format: int64
        recipe_id:
          type: integer
          format: int64
        item_id:
          type: integer
          format: int64
          description: 'Grand Exchange item ID, or `0` when it could not be

            resolved. A zero here means the input cannot be priced, and

            any path depending on it is reported as incomplete.

            '
        item_name:
          type: string
        quantity:
          type: integer
          description: Units consumed per craft.
        is_intermediate:
          type: boolean
          description: True when this input is itself craftable.
    RecipeListResponse:
      type: object
      required:
      - count
      - recipes
      properties:
        count:
          type: integer
        recipes:
          type: array
          items:
            $ref: '#/components/schemas/Recipe'
    RecipeSearchResponse:
      type: object
      required:
      - query
      - count
      - total
      - limit
      - offset
      - recipes
      properties:
        query:
          type: string
        count:
          type: integer
          description: Rows on this page.
        total:
          type: integer
          format: int64
          description: Rows matching across all pages.
        limit:
          type: integer
        offset:
          type: integer
        recipes:
          type: array
          items:
            $ref: '#/components/schemas/Recipe'
    RecipeTree:
      type: object
      required:
      - root
      properties:
        root:
          $ref: '#/components/schemas/RecipeTreeNode'
    RecipeTreeNode:
      type: object
      required:
      - recipe
      - depth
      properties:
        recipe:
          $ref: '#/components/schemas/Recipe'
        children:
          type:
          - array
          - 'null'
          description: Inputs that are themselves craftable.
          items:
            $ref: '#/components/schemas/RecipeTreeNode'
        depth:
          type: integer
