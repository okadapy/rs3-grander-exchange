# Generated by scripts/split_openapi.py — do not edit.
# Source: openapi/combined.yaml
openapi: 3.1.0
info:
  title: RS3 Market — gateway
  version: 2.0.0
  description: 'Endpoints served by **gateway**, extracted from the combined specification.


    This copy exists for debugging one service in isolation. Generate clients from `openapi/combined.yaml`,
    which describes the whole gateway surface.


    Generated by `scripts/split_openapi.py` — do not edit by hand.'
servers:
- url: /
  description: gateway
security: []
paths:
  /health:
    get:
      tags:
      - health
      operationId: getHealth
      summary: Gateway liveness
      responses:
        '200':
          description: Gateway is up.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthStatus'
  /health/all:
    get:
      tags:
      - health
      operationId: getUpstreamHealth
      summary: Liveness of every upstream service
      responses:
        '200':
          description: All upstreams healthy.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GatewayHealth'
        '503':
          description: At least one upstream is unhealthy.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GatewayHealth'
tags:
- name: health
  description: Liveness and upstream status.
components:
  schemas:
    GatewayHealth:
      type: object
      required:
      - gateway
      - all_upstreams_ok
      - upstreams
      properties:
        gateway:
          type: string
        all_upstreams_ok:
          type: boolean
        upstreams:
          type: array
          items:
            $ref: '#/components/schemas/UpstreamHealth'
    HealthStatus:
      type: object
      required:
      - status
      properties:
        status:
          type: string
          example: ok
        service:
          type: string
    UpstreamHealth:
      type: object
      required:
      - target
      - ok
      - ms
      properties:
        target:
          type: string
        ok:
          type: boolean
        status:
          type: integer
        error:
          type: string
        ms:
          type: integer
          format: int64
          description: Round-trip time of the probe.
